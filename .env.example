# =============================================================================
# Environment Variables Configuration
# =============================================================================
# Copy this file to .env.local and fill in your actual values
# Never commit .env.local or any .env files with real secrets!

# =============================================================================
# Core Next.js Configuration
# =============================================================================

# Environment mode (development, production, test)
NODE_ENV=development

# Base URL for the application (used for absolute URLs, redirects, etc.)
NEXT_PUBLIC_APP_URL=http://localhost:3000

# Server port (optional - Next.js defaults to 3000)
PORT=3000

# =============================================================================
# Email Service Configuration
# =============================================================================

# SMTP Configuration (if using SMTP)
# SMTP_HOST=smtp.gmail.com
# SMTP_PORT=587
# SMTP_USER=your-email@gmail.com
# SMTP_PASS=your-app-password

# Email Service API (if using services like SendGrid, Resend, etc.)
# EMAIL_SERVICE_API_KEY=your-api-key
# EMAIL_FROM_ADDRESS=noreply@yourdomain.com
# EMAIL_FROM_NAME=Your App Name

# =============================================================================
# Analytics & Monitoring
# =============================================================================

# Google Analytics
# NEXT_PUBLIC_GA_MEASUREMENT_ID=G-XXXXXXXXXX

# Plausible Analytics
# NEXT_PUBLIC_PLAUSIBLE_DOMAIN=yourdomain.com

# Sentry Error Tracking
# SENTRY_DSN=your-sentry-dsn
# NEXT_PUBLIC_SENTRY_DSN=your-public-sentry-dsn

# LogRocket Session Replay
# NEXT_PUBLIC_LOGROCKET_APP_ID=your-logrocket-id

# =============================================================================
# Feature Flags & Configuration
# =============================================================================

# Enable/disable features
# NEXT_PUBLIC_ENABLE_ANALYTICS=false
# NEXT_PUBLIC_ENABLE_DEBUG_MODE=true
# NEXT_PUBLIC_ENABLE_MAINTENANCE_MODE=false

# =============================================================================
# API & Rate Limiting
# =============================================================================

# API rate limiting
# RATE_LIMIT_MAX_REQUESTS=100
# RATE_LIMIT_WINDOW_MS=900000

# External API keys (if needed)
# EXTERNAL_API_KEY=your-external-api-key

# =============================================================================
# File Storage & Uploads
# =============================================================================

# File upload configuration
# MAX_FILE_SIZE=10485760
# ALLOWED_FILE_TYPES=image/jpeg,image/png,image/gif,application/pdf

# Cloud storage (if using AWS S3, Cloudinary, etc.)
# CLOUD_STORAGE_BUCKET=your-bucket-name
# CLOUD_STORAGE_REGION=us-east-1
# CLOUD_STORAGE_ACCESS_KEY=your-access-key
# CLOUD_STORAGE_SECRET_KEY=your-secret-key

# =============================================================================
# Security & Performance
# =============================================================================

# Security headers and CORS
# NEXT_PUBLIC_CORS_ORIGIN=http://localhost:3000

# Cache configuration
# CACHE_TTL=3600

# =============================================================================
# Development & Debugging
# =============================================================================

# Debug mode (only for development)
# DEBUG=true
# VERBOSE_LOGGING=false

# Mock data for development
# USE_MOCK_DATA=false

# Debug token for secure environment variable debugging (development only)
# DEBUG_TOKEN=your-secure-debug-token-here

# Supabase Configuration
# Get these values from your Supabase project dashboard
NEXT_PUBLIC_SUPABASE_URL=your_supabase_project_url
NEXT_PUBLIC_SUPABASE_ANON_KEY=your_supabase_anon_key

# Note: These are safe to expose publicly as they're protected by Row Level Security (RLS) policies
# The anon key only allows access to data that RLS policies permit

